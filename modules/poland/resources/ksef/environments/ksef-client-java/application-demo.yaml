sdk:
  config:
    base-uri: "https://api-demo.ksef.mf.gov.pl"
    qr-uri: "https://qr-demo.ksef.mf.gov.pl"
    request-timeout: 100
    default-headers:
      # Wymuszenie formatu Problem Details dla odpowiedzi błędów 400 i 429
      X-Error-Format: "problem-details"
